/**
 * Placeholder written for company — the one field an automatic write path (auto-tag,
 * resolve-and-upsert) can never know: which client a WhatsApp chat belongs to. Also what flags a
 * row incomplete in the dashboard table (see ClientMappings.tsx isIncompleteMapping). Centralised
 * here so every automatic writer agrees on the exact placeholder string.
 */
export const UNKNOWN_CLIENT_MAPPING_COMPANY = 'Unknown';
